const config = {
  db_info: {
    host: 'localhost',
    user: 'root',
    password: 'root123',
    port: 3306,
    database: 'boost',
    connectionLimit: 50,
    dateStrings: 'date',
    multipleStatements: true
  }
}

module.exports = config;